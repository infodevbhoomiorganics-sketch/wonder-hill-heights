import { createFileRoute } from "@tanstack/react-router";
import { X, ChevronLeft, ChevronRight } from "lucide-react";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { PageHero, SectionHeading } from "@/components/hotel-layout";
import { galleryImages, images, makeHead } from "@/lib/hotel";
export const Route=createFileRoute("/gallery")({head:()=>makeHead("Gallery | Hotel Wonder Hill Inn Narkanda","View rooms, mountain landscapes, dining and hotel spaces at Hotel Wonder Hill Inn Narkanda in Himachal Pradesh.","/gallery"),component:GalleryPage});
function GalleryPage(){const[selected,setSelected]=useState<number|null>(null);useEffect(()=>{if(selected===null)return;const onKey=(e:KeyboardEvent)=>{if(e.key==='Escape')setSelected(null);if(e.key==='ArrowRight')setSelected((selected+1)%galleryImages.length);if(e.key==='ArrowLeft')setSelected((selected-1+galleryImages.length)%galleryImages.length)};window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[selected]);const activeImage=selected===null?undefined:galleryImages[selected];return <><PageHero image={images.nh5View} eyebrow="Gallery" title="A closer look at Wonder Hill"/><section className="px-5 py-24 lg:px-8"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="The property & beyond" title="Rooms, ridges and mountain days."/><div className="mt-14 columns-1 gap-5 sm:columns-2 lg:columns-3">{galleryImages.map((img,i)=><button key={img.src} onClick={()=>setSelected(i)} className="image-reveal mb-5 block w-full cursor-zoom-in bg-muted"><img src={img.src} alt={img.alt} className="h-auto w-full object-cover"/></button>)}</div></div></section>{selected!==null&&activeImage&&<div className="fixed inset-0 z-[100] flex items-center justify-center bg-header/96 p-4 text-header-foreground" role="dialog" aria-modal="true" aria-label="Gallery image"><Button variant="ghost" size="icon" onClick={()=>setSelected(null)} className="absolute right-5 top-5 text-header-foreground" aria-label="Close lightbox"><X/></Button><Button variant="ghost" size="icon" onClick={()=>setSelected((selected-1+galleryImages.length)%galleryImages.length)} className="absolute left-3 top-1/2 text-header-foreground sm:left-6" aria-label="Previous image"><ChevronLeft/></Button><img src={activeImage.src} alt={activeImage.alt} className="max-h-[86vh] max-w-[88vw] object-contain"/><Button variant="ghost" size="icon" onClick={()=>setSelected((selected+1)%galleryImages.length)} className="absolute right-3 top-1/2 text-header-foreground sm:right-6" aria-label="Next image"><ChevronRight/></Button><p className="absolute bottom-5 text-xs uppercase tracking-[.15em] text-header-muted">{selected+1} / {galleryImages.length}</p></div>}</>}