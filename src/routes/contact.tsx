import { createFileRoute } from "@tanstack/react-router";
import { Mail, MapPin, Phone } from "lucide-react";
import { Button } from "@/components/ui/button";
import { PageHero, SectionHeading } from "@/components/hotel-layout";
import { hotel, images, makeHead } from "@/lib/hotel";
export const Route=createFileRoute("/contact")({head:()=>makeHead("Contact & Book Hotel Wonder Hill Inn Narkanda","Call or email Hotel Wonder Hill Inn Narkanda to check room availability and plan your mountain stay on NH-5.","/contact"),component:ContactPage});
function ContactPage(){return <><PageHero image={images.terraceView} eyebrow="Contact" title="Let’s plan your Narkanda stay"/><section className="px-5 py-24 lg:px-8 lg:py-32"><div className="mx-auto grid max-w-7xl gap-16 lg:grid-cols-2"><div><SectionHeading eyebrow="Direct enquiries" title="Speak with the hotel." copy="For availability, room details, dining questions or pet policy confirmation, contact the property directly."/><div className="mt-10 divide-y border-y"><div className="flex gap-4 py-6"><Phone className="text-accent"/><div><p className="text-xs uppercase tracking-[.14em] text-muted-foreground">Phone</p><a href={hotel.phone1Href} className="mt-2 block text-lg">{hotel.phone1}</a><a href={hotel.phone2Href} className="mt-1 block text-lg">{hotel.phone2}</a></div></div><div className="flex gap-4 py-6"><Mail className="text-accent"/><div><p className="text-xs uppercase tracking-[.14em] text-muted-foreground">Email</p><a href={`mailto:${hotel.email}`} className="mt-2 block text-lg">{hotel.email}</a></div></div><div className="flex gap-4 py-6"><MapPin className="text-accent"/><div><p className="text-xs uppercase tracking-[.14em] text-muted-foreground">Address</p><p className="mt-2 leading-7">{hotel.address}</p></div></div></div></div><div className="bg-primary p-7 text-primary-foreground sm:p-12"><p className="eyebrow text-accent">Enquire now</p><h2 className="mt-3 font-display text-5xl">Your mountain room is a call away.</h2><p className="mt-5 leading-8 text-header-muted">Call either hotel number for current availability and the most suitable room category for your stay.</p><div className="mt-9 grid gap-3"><Button asChild variant="gold" size="lg" className="w-full"><a href={hotel.phone1Href}><Phone/> Call {hotel.phone1}</a></Button><Button asChild variant="hotelOutline" size="lg" className="w-full"><a href={hotel.phone2Href}><Phone/> Call {hotel.phone2}</a></Button><Button asChild variant="hotelOutline" size="lg" className="w-full"><a href={`mailto:${hotel.email}?subject=Stay enquiry for Hotel Wonder Hill Inn`}><Mail/> Send Enquiry</a></Button></div><p className="mt-6 text-xs leading-6 text-header-muted">Room prices vary by season, room type and booking platform. Please confirm current rates when enquiring.</p></div></div></section></>}